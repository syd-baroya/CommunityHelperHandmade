<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.saga.communityhelperhandmade">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="secapstone.helper.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="secapstone.helper.MainActivity" />
        <activity android:name="secapstone.helper.AddArtisanActivity" />
        <activity android:name="secapstone.helper.addartisan.WelcomeAddArtisanActivity" />
        <activity android:name="secapstone.helper.addartisan.NameAddArtisanActivity" />
        <activity android:name="secapstone.helper.addartisan.PhotoAddArtisanActivity" />
        <activity android:name="secapstone.helper.addartisan.PhoneNumberAddArtisanActivity" />
        <activity android:name="secapstone.helper.addartisan.MapAddArtisanActivity" />
        <activity android:name="secapstone.helper.addartisan.DescriptionAddArtisanActivity" />
        <activity android:name="secapstone.helper.addartisan.SignatureAddArtisanActivity" />
        <activity android:name="secapstone.helper.DashBoard" />
        <activity android:name="secapstone.helper.ForgotPassword"></activity>
    </application>

</manifest>